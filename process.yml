apps:
  - name: sandu
    script: keystone.js
    instances: 0
    exec_mode: cluster
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
    log_date_format: YYYY-MM-DD HH:mm:ss.SSS Z
    merge_logs: true
